# What Are Agent Skills

Local agent skills are reusable folders that help a coding agent follow the same instructions across repeated tasks.

A skill is usually a folder.
`SKILL.md` contains the main instructions.
Scripts and other resources can be added when useful.

The goal is simple: make repeated workflows easier to reuse without rewriting the same long prompt every time.
