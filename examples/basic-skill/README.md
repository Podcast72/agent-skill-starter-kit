# Basic Skill Example

Copy this folder when you want the smallest possible starting point.

Suggested steps:

1. Rename the folder.
2. Update the `name` and `description` fields in `SKILL.md`.
3. Rewrite the activation line and rules for your workflow.
4. Test the skill on one small task before expanding it.
