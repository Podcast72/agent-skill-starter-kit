# Experimental Passive Skill Example

This example shows a passive logging pattern with strict limits.

The logging script records only small metadata fields for later manual review. It does not store full prompts, file contents, secrets or personal data.

Use this pattern only when minimal usage signals are genuinely useful and easy to review.
